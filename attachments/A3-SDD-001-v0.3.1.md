# Thiết kế phần mềm Arena3 — hợp đồng kỹ thuật
## Arena3 Software Design Description (binding)

- Mã: **A3-SDD-001** v0.3.1 (Binding)
- Ngày: 12/09/2026
- Stack: PostgreSQL 16 + btree_gist · một backend HTTP JSON · PWA React
- Đi kèm: A3-SRS-001 v1.3.1

## Slice 1
In: Auth (login, OTP stub, phiên); User/member + search không dấu; Gói + subscription + thu tiền quầy; Court + occupancy + session (lớp chiếm sân); Hold / confirm / cancel / walk-in / no-show job; Payment cash/transfer, chứng từ bất biến, PDF đơn giản; Báo cáo doanh thu ngày; center_settings
Out: F4 điểm danh / giáo án; F5/F6 AI; Cổng thanh toán; SMS vendor thật (OTP ghi log staging); Waitlist mời 2h; Dụng cụ; Đóng băng gói; Convert BR↔BC UI (cột pair_court_id vẫn có trong schema); Hóa đơn điện tử
Gates: TC-08, TC-14, TC-16, TC-22, TC-04, TC-05, TC-25

# 1. Mục đích và ràng buộc

Tài liệu này là hợp đồng kỹ thuật (SDD) đi kèm A3-SRS-001 v1.3.1. SRS nói hệ thống phải đúng gì. SDD khóa hệ thống được lắp thế nào: schema, occupancy, API, job, state machine, stack. AI/dev không được bịa bảng, path, hay job ngoài danh mục này.

> **Stack chốt**
> PostgreSQL 16 + btree_gist + unaccent. Một backend HTTP JSON /v1. PWA React. Tiền tệ VND, TZ Asia/Ho_Chi_Minh, làm tròn 1.000đ. Không multi-tenant. Không bán offline.

### 1.1 Slice 1 — lần generate đầu

- In: Auth (login, OTP stub, phiên)
- In: User/member + search không dấu
- In: Gói + subscription + thu tiền quầy
- In: Court + occupancy + session (lớp chiếm sân)
- In: Hold / confirm / cancel / walk-in / no-show job
- In: Payment cash/transfer, chứng từ bất biến, PDF đơn giản
- In: Báo cáo doanh thu ngày
- In: center_settings

- Out: F4 điểm danh / giáo án
- Out: F5/F6 AI
- Out: Cổng thanh toán
- Out: SMS vendor thật (OTP ghi log staging)
- Out: Waitlist mời 2h
- Out: Dụng cụ
- Out: Đóng băng gói
- Out: Convert BR↔BC UI (cột pair_court_id vẫn có trong schema)
- Out: Hóa đơn điện tử

Cổng kiểm: mỗi PR slice 1 phải chạy TC-08 (lớp đè sân), TC-14 (20 request cùng slot), TC-22 (không xóa phiếu), TC-04/05 (gia hạn), TC-25 (member không đọc báo cáo).

# 2. Khóa lịch một chỗ

Lỗ chết người của SRS v1.2 DDL: exclusion tách trên sessions và court_bookings — lớp vẫn đè sân. SDD sửa: một bảng occupancies là nguồn sự thật. Session, booking, hold, bảo trì, convert chỉ được tồn tại nếu INSERT occupancy thành công trong cùng transaction.

*[Sơ đồ: occupancy]*

1. Cấm ghi occupancies trực tiếp. Chỉ function occupancy_attach / confirm_hold / release / attach_convert.
2. Exclusion trên mọi hàng đang tồn tại — terminal state PHẢI release (xóa hàng) nếu không slot chết.
3. hold→confirmed: occupancy_confirm_hold (đổi kind, cùng id). Cấm attach hàng thứ hai.
4. Booking occupying ⇒ occupancy_id NOT NULL UNIQUE. Terminal ⇒ NULL sau release.
5. Không bảng court_blocks. Bảo trì = attach kind=maintenance.
6. Convert: occupancy_attach_convert = đúng 2 hàng (sân + pair_court_id) cùng convert_group_id.
7. HLV chính + phụ: mỗi người một coach_occupancies (FK session).
8. UUID booking/session sinh trước, attach occupancy với ref_id đó, rồi INSERT nghiệp vụ kèm occupancy_id.

> **Cấm**
> Cấm ‘kiểm tra trống rồi insert’ không qua occupancies. Cấm SERIALIZABLE như biện pháp thay exclusion. Cấm dual-write occupancy sau khi đã commit booking.

# 3. Schema đủ

DDL đầy đủ ở tab Schema (v0.3.1). booking_replace_hold, complete_bookings, unique ca/session, REVOKE thật, outbox-only, E.164.

| Bảng | Khóa vấn đề SRS |
| --- | --- |
| occupancies + occupancy_*() | Lớp + sân + hold + bảo trì + convert; cấm dual-write |
| price_rules | Cao/thấp điểm, snapshot lúc confirm |
| center_settings | Giờ mở, TTL hold, trần nợ, cửa sổ hủy |
| occupancy kind=maintenance | BR-36 — không bảng court_blocks |
| waitlist_offers + unique pos | BR-25; không trùng vị trí |
| equipment_loans | BR-39E nợ dụng cụ |
| invoices + lines | PDF dòng hàng, MST; 1 payment = 1 invoice |
| payments.status + shifts | refund_pending ≥1tr; đối soát ca BR-49 |
| one_live_sub_per_scope | BR-19 Active|frozen / user / môn |
| enrolled_count + enrollment_confirm() | Khóa sĩ số TC-10 |

status Subscription: pending|active|expired|frozen. Không lưu expiring. Job chỉ active→expired khi end_on < today. Unique (user_id, sport_scope) WHERE active|frozen.

# 4. API Must

Danh mục endpoint tab API. Không path lạ. Prefix /v1. JSON. Lỗi { code, message, br? }. Idempotency-Key bắt buộc trên POST bookings, confirm, payments, walk-in.

### 4.1 Biên giao dịch confirm sân

1. BEGIN
2. SELECT booking FOR UPDATE WHERE status=hold AND hold_until > now()
3. Ghi payment/invoice hoặc trừ quota
4. UPDATE occupancy.kind hold → booking (cùng hàng)
5. UPDATE booking confirmed + snapshot giá
6. COMMIT — fail bất kỳ → ROLLBACK, hold còn đến TTL

Phiên: token opaque (hash trên DB). TV 7 ngày, nhân sự 12 giờ. Cookie httpOnly hoặc Bearer — chọn một, không mix. OTP 6 số TTL 5 phút, khóa 5 lần/15 phút.

# 5. Job và thông báo

Mọi job idempotent, TZ ICT, có dead-letter. Chi tiết tab Jobs. Slice 1 bắt buộc: expire_holds, mark_noshow, subscription_status, expiry_reminders, generate_sessions, notify_flush.

- In-app bắt buộc (bảng notifications).
- SMS adapter — interface gửi; stub ghi log ở slice 1.
- Template khóa: booking_confirmed, class_changed, sub_expiring, payment_receipt.
- Không nhét gửi SMS trong request HTTP đặt sân (chỉ outbox).

# 6. State machine

Bốn máy trạng thái ràng API: Subscription, CourtBooking, Enrollment, Payment. Cấm chuyển không có cạnh.

*[Sơ đồ: sub-state]*

*[Sơ đồ: booking-state]*

*[Sơ đồ: enroll-state]*

*[Sơ đồ: pay-state]*

# 7. Protocol cho AI / PR

1. Nguồn sự thật yêu cầu = SRS. Nguồn sự thật lắp ráp = SDD này.
2. Cấm tạo bảng/path/job không có trong SDD. Thiếu thì hỏi, không bịa.
3. Mỗi PR slice 1: TC-08, TC-14, TC-22 phải xanh (kể cả test đồng thời).
4. Không implement F4–F6, cổng thanh toán, SMS vendor, waitlist trong slice 1.
5. Mã lỗi đúng enum. 409 CONFLICT_SLOT khi occupancy đụng.
6. Sinh mã A3/CRT/PAY qua code_counters (UPDATE n = n+1 RETURNING), không MAX()+1.
7. Search TV: name_normalized = lower(unaccent(full_name)).
8. REVOKE INSERT/UPDATE/DELETE occupancies thật (role arena3_app). Backend chỉ connect role đó.
9. openapi.yaml SINH từ ENDPOINTS slice 1 — thiếu path = không implement.
10. Hold: booking_replace_hold (TC-16). Job complete_bookings cho in_use→completed.
11. Bearer only. Phone E.164 + next_member_code. Chỉ outbox (inbox view).
12. Job hủy/no-show/completed chỉ occupancy_release_booking|session.

> **Figma**
> Không chặn go-live quầy. Toàn vẹn lịch + chứng từ mới là nút thắt. UI bám tab Màn hình SRS.

# 8. Quyết định — cấm AI đoán

| Mục | Quyết định slice 1 |
| --- | --- |
| Pending cùng scope | Unique pending (user, sport_scope). Mua khi đang pending = cập nhật đơn cũ. |
| Gia hạn | Cùng hàng subscription: cộng end_on BR-10/11 + quota. Payment ref_id = hàng đó. Không INSERT song song. |
| Quota sân | INSERT payments method=quota amount_vnd=0 status=posted. DT = method<>quota AND posted. Dòng riêng SUM(quota_hours). |
| Walk-in SĐT | Tra users.phone; trùng → user_id. Một SĐT một hold mọi kênh. |
| Lớp 90’ vs slot 60’ | Occupancy = start_at + duration_min. Heatmap ô 60’ bận nếu giao [start,end). Không làm tròn lớp thành 2 slot. |
| BR-39C | POST /bookings chồng lớp của user → 409 requires_confirm. Client gửi confirm_overlap=true mới hold. |
| GET /classes | public, không PII. POST enroll = staff_or_member (staff gửi user_id). |
| POST /payments | desk = LT|Manager. LT bắt shift_id ca open; Manager không. |
| Phiên | Bearer only. TV 7 ngày / nhân sự 12 giờ trên sessions_auth.expires_at. |
| Notify | Chỉ outbox. inbox = view channel=inapp AND sent_at NOT NULL. |
| rrule | Subset FREQ=WEEKLY;BYDAY;BYHOUR + duration_min. Job generate chỉ nhận subset này. |
| Giá chồng khung | EXCLUDE price_rules. Lookup: rule court_id thắng rule sport. |

# Occupancy rules
- Backend CHỈ connect role arena3_app. REVOKE INSERT/UPDATE/DELETE occupancies — chỉ EXECUTE occupancy_* + booking_replace_hold.
- Hold mới: booking_replace_hold — hủy hold cũ cùng TX (TC-16). Cấm occupancy_attach hold trực tiếp từ API.
- Hủy / no-show / completed: occupancy_release_booking (NULL occupancy_id rồi DELETE).
- Job complete_bookings: in_use AND end_at+ân hạn < now() → occupancy_release_booking(..., completed).
- Convert: attach_convert; exception xóa convert_group rồi CONFLICT_SLOT.
- Session UNIQUE (class_id, start_at). Generate conflict = skip.
- Một ca open / lễ tân (one_open_shift).
- Phone lưu E.164 qua normalize_phone. member_code = next_member_code().
- Giá holiday: seed copy weekend; lookup holiday → weekend → weekday.
- Notify: chỉ outbox. inbox = view sent inapp. Cấm bảng notifications.

# Errors
| Code | HTTP | Meaning |
| --- | --- | --- |
| UNAUTHENTICATED | 401 | Thiếu/hết phiên |
| FORBIDDEN | 403 | Sai vai trò / flag tắt |
| NOT_FOUND | 404 | Không có tài nguyên |
| CONFLICT_SLOT | 409 | Occupancy đụng; kèm gợi ý sân kề |
| HOLD_EXPIRED | 409 | Hold quá TTL |
| CONFLICT_STATE | 409 | Sai trạng thái (confirm booking cancelled) |
| BR_VIOLATION | 422 | { br, message } — vi phạm quy tắc |
| VALIDATION | 422 | Field sai kiểu/thiếu |
| RATE_LIMITED | 429 | OTP / login |

Header Idempotency-Key (UUID) bắt buộc trên POST /bookings, /bookings/:id/confirm, /payments, /walk-in. Replay cùng key+hash → trả response đã lưu, không insert lần 2. Key TTL 24h.

# Confirm transaction
1. BEGIN
2. Khóa hàng booking status=hold AND hold_until > now() (SELECT FOR UPDATE)
3. Ghi payment: tiền → posted; quota → method=quota amount_vnd=0 status=posted (không cộng DT)
4. occupancy_confirm_hold(booking.occupancy_id)
5. UPDATE booking SET status='confirmed', price_vnd snapshot
6. COMMIT
7. Fail bất kỳ bước → ROLLBACK, hold còn nguyên đến TTL

# Endpoints
- `POST /v1/auth/register` [s1] Chỉ lưu otp_challenges (chưa INSERT users) — auth public — BR BR-01,BR-07,BR-08
- `POST /v1/auth/otp/verify` [s1] OTP đúng mới INSERT users + phiên — auth public — BR BR-01
- `POST /v1/auth/login` [s1] Đăng nhập SĐT/email + MK — auth public — BR BR-02,BR-04
- `POST /v1/auth/logout` [s1] Hủy phiên — auth authenticated — BR —
- `POST /v1/auth/password/forgot` [s1] OTP quên MK (otp_challenges purpose=reset) — auth public — BR BR-04
- `GET /v1/me` [s1] Hồ sơ + gói active — auth authenticated — BR —
- `PATCH /v1/me` [s1] Sửa tên/health. Cấm phone/email (slice 2: POST /me/contact + OTP) — auth authenticated — BR BR-01
- `GET /v1/members` [s1] Tìm TV không dấu, từ 3 ký tự — auth staff — BR —
- `POST /v1/members` [s1] Tạo TV tại quầy — auth receptionist — BR BR-01,BR-07,BR-08
- `GET /v1/members/:id` [s1] Hồ sơ quầy: hạn, lịch hôm nay, nợ — auth staff — BR BR-12,BR-44
- `GET /v1/plans` [s1] Gói đang bán (TV) / tất cả (Manager) — auth public — BR —
- `POST /v1/plans` [s1] Tạo gói — auth manager — BR BR-65
- `PATCH /v1/plans/:id` [s1] Sửa gói — không hồi tố subscription cũ — auth manager — BR BR-65
- `POST /v1/subscriptions` [s1] Pending cùng scope = cập nhật đơn cũ, không INSERT thứ hai — auth member — BR BR-10,BR-11,BR-19
- `POST /v1/subscriptions/:id/freeze` [s2] Đóng băng gói — auth manager — BR BR-14,BR-15
- `GET /v1/courts` [s1] Danh sách sân — auth public — BR —
- `GET /v1/occupancy` [s1] Heatmap ngày — mọi vai trò đã đăng nhập — auth authenticated — BR BR-21
- `POST /v1/bookings` [s1] Hold qua booking_replace_hold (hủy hold cũ cùng TX — TC-16) — auth member — BR BR-31,BR-32,BR-39,BR-39C
- `POST /v1/bookings/:id/confirm` [s1] Pay/quota (method=quota amount=0 posted, không cộng DT), hold→booking — auth member — BR BR-34,BR-39D,BR-48
- `POST /v1/bookings/:id/cancel` [s1] Hủy theo BR-33 — auth member — BR BR-33
- `POST /v1/bookings/:id/check-in` [s1] Cửa sổ −15/+10 — auth receptionist — BR BR-39B
- `POST /v1/walk-in` [s1] Nếu guest_phone = users.phone thì gắn user_id (một hold / SĐT) — auth receptionist — BR BR-39A,BR-66
- `POST /v1/shifts/open` [s1] Mở ca quầy — auth receptionist — BR BR-49
- `GET /v1/shifts/current` [s1] Ca đang open của lễ tân — auth receptionist — BR BR-49
- `POST /v1/shifts/:id/close` [s1] Đóng ca + cash_declared — auth receptionist — BR BR-49
- `GET /v1/classes` [s1] Catalog lớp open — public, không PII HV — auth public — BR BR-67
- `POST /v1/classes` [s1] Tạo lớp nháp — auth manager — BR BR-21,BR-23
- `POST /v1/classes/:id/publish` [s1] Generate session + occupancy + coach_occupancy — auth manager — BR BR-21,BR-67
- `POST /v1/classes/:id/enroll` [s1] enrollment_confirm. Staff gửi user_id; TV tự ghi danh — auth staff_or_member — BR BR-12,BR-16,BR-18,BR-24
- `DELETE /v1/enrollments/:id` [s1] Hủy ghi danh ≥ 4h — auth member — BR BR-20
- `POST /v1/waitlist/:id/accept` [s2] Nhận chỗ mời — auth member — BR BR-25
- `POST /v1/payments` [s1] Thu pending. LT bắt buộc shift_id ca open; Manager không cần ca — auth desk — BR BR-40,BR-43,BR-69
- `POST /v1/payments/:id/refund` [s1] <1tr posted âm; ≥1tr refund_pending (không DELETE) — auth receptionist — BR BR-41,BR-40
- `POST /v1/payments/:id/approve-refund` [s1] refund_pending → posted — auth manager — BR BR-41
- `POST /v1/payments/:id/reject-refund` [s1] refund_pending → refund_rejected; không vào DT; cấm DELETE — auth manager — BR BR-40,BR-41
- `GET /v1/invoices/:id.pdf` [s1] PDF A5 / 80mm ?format= — auth staff — BR BR-42
- `GET /v1/reports/revenue` [s1] Doanh thu kỳ; tách quota — auth manager — BR BR-48,BR-49
- `GET /v1/reports/occupancy` [s1] Công suất sân — auth manager — BR —
- `GET /v1/settings` [s1] Đọc center_settings — auth staff — BR —
- `PATCH /v1/settings` [s1] Sửa cấu hình — không hồi tố — auth manager — BR BR-65
- `GET /v1/price-rules` [s1] Bảng giá — auth public — BR BR-34
- `PUT /v1/price-rules` [s1] Thay bộ giá (giao dịch mới) — auth manager — BR BR-65

# Jobs
## expire_holds (every 15s, slice 1)
- SELECT hold hết hạn FOR UPDATE.
- occupancy_release_booking(id, 'cancelled') — NULL occupancy_id rồi DELETE occupancy.
- Idempotent: UPDATE bookings hold WHERE hold_until < now() — chạy lại không double-delete.

## mark_noshow (every 1m, slice 1)
- Booking confirmed AND start_at + noshow_grace < now() AND chưa check-in.
- occupancy_release_booking(id, 'no_show'). Không hoàn.
- Idempotent: Chỉ đụng status=confirmed.

## complete_bookings (every 1m, slice 1)
- in_use AND end_at + ân hạn (settings) < now().
- occupancy_release_booking(id, 'completed').
- Idempotent: Chỉ status=in_use.

## subscription_status (0 0 * * *, slice 1)
- frozen giữ nguyên.
- status=active AND end_on < today → expired.
- Không set expiring. API danh sách ‘sắp hết hạn’ = active AND end_on <= today+7.
- Idempotent: Chỉ active|frozen. expired suy từ end_on. Không ghi status=expiring.

## expiry_reminders (0 8 * * *, slice 1)
- Mốc 7, 3, 0 ngày (settings). In-app bắt buộc. SMS nếu flag.
- Idempotent: Một notification/user/mốc/ngày (unique key).

## generate_sessions (0 1 * * *, slice 1)
- Với class open, rrule subset RFC5545 FREQ=WEEKLY;BYDAY;BYHOUR + duration_min → 14 ngày tới.
- UUID session trước → occupancy_attach kind=session đúng start+duration (90’ không làm tròn 60’).
- Conflict sân/HLV → không tạo, outbox Manager.
- Idempotent: Không tạo session nếu đã có class_id+start_at.

## waitlist_expire (every 1m, slice 2)
- Hết hạn mời → skipped; tạo offer mới cho enrollment kế.
- Idempotent: Offer pending quá expires_at → expired, mời pos+1.

## lock_attendance (every 10m, slice 2)
- status=done; cấm HLV sửa.
- Idempotent: Session done khi end_at+2h.

## notify_flush (every 10s, slice 1)
- SELECT outbox WHERE sent_at IS NULL.
- Gửi in-app / SMS stub.
- SET sent_at. attempts < 5.
- sub_expiring: dedupe_key = {user_id}|{mốc}|{YYYY-MM-DD}.
- Idempotent: outbox.dedupe_key UNIQUE; chỉ sent_at IS NULL.

# Templates
- booking_confirmed: Confirm sân (in-app)
- booking_cancelled: Hủy sân (in-app)
- hold_expiring: Hold còn 60s (in-app)
- class_changed: Dời/hủy buổi (in-app (+SMS opt))
- sub_expiring: Mốc 7/3/0 (in-app (+SMS opt))
- waitlist_offer: Có chỗ (in-app)
- payment_receipt: Posted (in-app)

# State machines
## Subscription
- pending → active : thanh toán đủ (hoặc cọc BR-45)
- active → expired : job nightly khi end_on < today
- expired → active : gia hạn BR-11
- active → active : gia hạn BR-10 (đổi end_on)
- active → frozen : Manager
- frozen → active : hết freeze / Manager
Không có status expiring. UI lọc active AND end_on≤today+7. Frozen không bị job đè. Unique (user, sport_scope) với active|frozen.

## CourtBooking + occupancy.kind
- hold → confirmed : confirm + pay/quota
- hold → cancelled : hết TTL / user hủy
- confirmed → cancelled : hủy ≥ 2h + hoàn
- confirmed → in_use : check-in cửa sổ
- confirmed → no_show : +10 phút
- in_use → completed : hết slot + ân hạn
Không nhảy ngược. Confirm fail giữ hold.

## Enrollment
- → confirmed : còn chỗ + BR
- → waitlisted : đầy (slice 2)
- waitlisted → confirmed : nhận offer
- waitlisted → cancelled : hết hạn offer / bỏ
- confirmed → cancelled : hủy ≥ 4h (trả chỗ)
Slice 1 chỉ confirmed|cancelled.

## Payment
- → posted : thu / quota 0đ / hoàn < 1tr
- → refund_pending : lễ tân hoàn ≥ 1tr
- refund_pending → posted : Manager duyệt
- refund_pending → refund_rejected : Manager từ chối — cấm DELETE
Không DELETE. TC-22.

# DDL
```sql
-- Arena3 SDD v0.3.1 — PostgreSQL 16 — kèm A3-SRS-001 v1.3.1
-- Occupancy: chỉ occupancy_* . Job hủy/no-show gọi occupancy_release_booking/session (NULL occupancy_id TRƯỚC, rồi DELETE).

CREATE EXTENSION IF NOT EXISTS btree_gist;
CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE EXTENSION IF NOT EXISTS unaccent;

CREATE TYPE user_role AS ENUM ('manager','coach','receptionist','member');
CREATE TYPE user_status AS ENUM ('active','locked','disabled');
CREATE TYPE sport_kind AS ENUM ('badminton','basketball','volleyball','all');
CREATE TYPE sub_status AS ENUM ('pending','active','expired','frozen'); -- KHÔNG có expiring (tính từ end_on)
CREATE TYPE court_status AS ENUM ('ready','maintenance','closed');
CREATE TYPE class_status AS ENUM ('draft','open','closed','cancelled');
CREATE TYPE session_status AS ENUM ('scheduled','done','cancelled');
CREATE TYPE enroll_status AS ENUM ('confirmed','waitlisted','cancelled');
CREATE TYPE booking_status AS ENUM ('hold','confirmed','in_use','completed','cancelled','no_show');
CREATE TYPE pay_method AS ENUM ('cash','transfer','card','gateway','quota');
CREATE TYPE pay_status AS ENUM ('posted','refund_pending','refund_rejected');
CREATE TYPE occ_kind AS ENUM ('session','booking','hold','maintenance','convert');
CREATE TYPE att_kind AS ENUM ('session','gate');
CREATE TYPE att_result AS ENUM ('present','late','absent','excused');
CREATE TYPE loan_status AS ENUM ('out','returned','lost');
CREATE TYPE offer_status AS ENUM ('pending','accepted','expired','skipped');

CREATE TABLE center_settings (
  id SMALLINT PRIMARY KEY DEFAULT 1 CHECK (id = 1),
  timezone TEXT NOT NULL DEFAULT 'Asia/Ho_Chi_Minh',
  currency CHAR(3) NOT NULL DEFAULT 'VND',
  open_time TIME NOT NULL DEFAULT '06:00',
  close_time TIME NOT NULL DEFAULT '22:00',
  slot_minutes INT NOT NULL DEFAULT 60,
  hold_minutes INT NOT NULL DEFAULT 5,
  book_ahead_days INT NOT NULL DEFAULT 7,
  max_slots_per_day INT NOT NULL DEFAULT 2,
  cancel_court_hours INT NOT NULL DEFAULT 2,
  cancel_class_hours INT NOT NULL DEFAULT 4,
  noshow_grace_minutes INT NOT NULL DEFAULT 10,
  checkin_before_minutes INT NOT NULL DEFAULT 15,
  debt_limit_vnd INT NOT NULL DEFAULT 500000,
  refund_manager_vnd INT NOT NULL DEFAULT 1000000,
  freeze_max_days_year INT NOT NULL DEFAULT 30,
  minor_age INT NOT NULL DEFAULT 16,
  vat_rate NUMERIC(5,2) NOT NULL DEFAULT 0,
  round_vnd INT NOT NULL DEFAULT 1000,
  waitlist_offer_hours INT NOT NULL DEFAULT 2,
  deposit_pct_activates INT,
  tax_code VARCHAR(20),
  legal_name VARCHAR(190),
  address TEXT
);
INSERT INTO center_settings (id) VALUES (1);

CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  member_code VARCHAR(16) UNIQUE,
  full_name VARCHAR(120) NOT NULL,
  name_normalized VARCHAR(120) NOT NULL,
  phone VARCHAR(16) NOT NULL UNIQUE,
  email VARCHAR(190) UNIQUE,
  role user_role NOT NULL,
  status user_status NOT NULL DEFAULT 'active',
  password_hash TEXT NOT NULL,
  must_change_password BOOLEAN NOT NULL DEFAULT false,
  failed_logins INT NOT NULL DEFAULT 0,
  locked_until TIMESTAMPTZ,
  date_of_birth DATE,
  guardian_name VARCHAR(120),
  guardian_phone VARCHAR(16),
  national_id VARCHAR(20),
  pii_consent_at TIMESTAMPTZ,
  health_notes TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX users_name_norm_idx ON users (name_normalized);

CREATE TABLE coach_sports (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  sport sport_kind NOT NULL,
  PRIMARY KEY (user_id, sport)
);

CREATE TABLE membership_plans (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(80) NOT NULL,
  sport_scope sport_kind NOT NULL,
  duration_days INT,
  session_quota INT,
  court_hours INT NOT NULL DEFAULT 0,
  court_discount_pct INT NOT NULL DEFAULT 0 CHECK (court_discount_pct BETWEEN 0 AND 100),
  price_vnd INT NOT NULL CHECK (price_vnd >= 0),
  is_on_sale BOOLEAN NOT NULL DEFAULT true,
  carry_over_hours BOOLEAN NOT NULL DEFAULT false
);

CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  plan_id UUID NOT NULL REFERENCES membership_plans(id),
  sport_scope sport_kind NOT NULL,
  start_on DATE NOT NULL,
  end_on DATE NOT NULL,
  status sub_status NOT NULL DEFAULT 'pending',
  court_hours_left NUMERIC(6,1) NOT NULL DEFAULT 0,
  session_left INT,
  frozen_days INT NOT NULL DEFAULT 0
);
-- BR-19: một gói đang dùng / user / scope (active|frozen). pending không đếm.
CREATE UNIQUE INDEX one_live_sub_per_scope ON subscriptions (user_id, sport_scope)
  WHERE status IN ('active','frozen');
CREATE UNIQUE INDEX one_pending_sub_per_scope ON subscriptions (user_id, sport_scope)
  WHERE status = 'pending';

CREATE TABLE courts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  court_code VARCHAR(8) NOT NULL UNIQUE,
  sport sport_kind NOT NULL,
  status court_status NOT NULL DEFAULT 'ready',
  convertible BOOLEAN NOT NULL DEFAULT false,
  pair_court_id UUID REFERENCES courts(id),
  CONSTRAINT pair_distinct CHECK (pair_court_id IS NULL OR pair_court_id <> id)
);

CREATE TABLE occupancies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  court_id UUID NOT NULL REFERENCES courts(id),
  start_at TIMESTAMPTZ NOT NULL,
  end_at TIMESTAMPTZ NOT NULL,
  kind occ_kind NOT NULL,
  ref_id UUID NOT NULL,
  convert_group_id UUID,
  reason TEXT,
  CONSTRAINT occ_range CHECK (end_at > start_at),
  CONSTRAINT convert_group_chk CHECK (
    (kind = 'convert' AND convert_group_id IS NOT NULL)
    OR (kind <> 'convert' AND convert_group_id IS NULL)
  ),
  EXCLUDE USING gist (
    court_id WITH =,
    tstzrange(start_at, end_at, '[)') WITH &&
  )
);
CREATE UNIQUE INDEX occ_kind_ref_court ON occupancies (kind, ref_id, court_id);
CREATE INDEX occupancies_day_idx ON occupancies (court_id, start_at);

CREATE TABLE coach_occupancies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  coach_id UUID NOT NULL REFERENCES users(id),
  session_id UUID NOT NULL,
  start_at TIMESTAMPTZ NOT NULL,
  end_at TIMESTAMPTZ NOT NULL,
  CONSTRAINT coach_occ_range CHECK (end_at > start_at),
  EXCLUDE USING gist (
    coach_id WITH =,
    tstzrange(start_at, end_at, '[)') WITH &&
  )
);

CREATE TABLE price_rules (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sport sport_kind NOT NULL,
  court_id UUID REFERENCES courts(id),
  day_kind VARCHAR(16) NOT NULL CHECK (day_kind IN ('weekday','weekend','holiday')),
  start_local TIME NOT NULL,
  end_local TIME NOT NULL,
  price_vnd INT NOT NULL CHECK (price_vnd >= 0),
  is_peak BOOLEAN NOT NULL DEFAULT false,
  start_min INT GENERATED ALWAYS AS ((EXTRACT(HOUR FROM start_local)::int * 60) + EXTRACT(MINUTE FROM start_local)::int) STORED,
  end_min INT GENERATED ALWAYS AS ((EXTRACT(HOUR FROM end_local)::int * 60) + EXTRACT(MINUTE FROM end_local)::int) STORED,
  CONSTRAINT price_range CHECK (end_local > start_local),
  EXCLUDE USING gist (
    sport WITH =,
    COALESCE(court_id, '00000000-0000-0000-0000-000000000000'::uuid) WITH =,
    day_kind WITH =,
    int4range(start_min, end_min, '[)') WITH &&
  )
);

CREATE TABLE classes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sport sport_kind NOT NULL,
  level VARCHAR(24) NOT NULL,
  coach_id UUID NOT NULL REFERENCES users(id),
  assistant_id UUID REFERENCES users(id),
  court_id UUID NOT NULL REFERENCES courts(id),
  capacity INT NOT NULL CHECK (capacity > 0),
  enrolled_count INT NOT NULL DEFAULT 0,
  rrule TEXT NOT NULL,
  duration_min INT NOT NULL DEFAULT 90,
  start_on DATE NOT NULL,
  end_on DATE NOT NULL,
  status class_status NOT NULL DEFAULT 'draft',
  CONSTRAINT enrolled_cap CHECK (enrolled_count >= 0 AND enrolled_count <= capacity)
);

CREATE TABLE sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_id UUID NOT NULL REFERENCES classes(id),
  court_id UUID NOT NULL REFERENCES courts(id),
  start_at TIMESTAMPTZ NOT NULL,
  end_at TIMESTAMPTZ NOT NULL,
  status session_status NOT NULL DEFAULT 'scheduled',
  occupancy_id UUID UNIQUE REFERENCES occupancies(id),
  CONSTRAINT session_occ_sync CHECK (
    (status = 'scheduled' AND occupancy_id IS NOT NULL)
    OR (status IN ('done','cancelled') AND occupancy_id IS NULL)
  ),
  UNIQUE (class_id, start_at)
);
ALTER TABLE coach_occupancies
  ADD CONSTRAINT coach_occ_session_fk FOREIGN KEY (session_id) REFERENCES sessions(id) DEFERRABLE INITIALLY DEFERRED;

CREATE TABLE enrollments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_id UUID NOT NULL REFERENCES classes(id),
  user_id UUID NOT NULL REFERENCES users(id),
  status enroll_status NOT NULL,
  waitlist_pos INT,
  UNIQUE (class_id, user_id),
  CONSTRAINT waitlist_pos_chk CHECK (
    (status = 'waitlisted' AND waitlist_pos IS NOT NULL)
    OR (status <> 'waitlisted' AND waitlist_pos IS NULL)
  )
);
CREATE UNIQUE INDEX enrollments_waitlist_pos ON enrollments (class_id, waitlist_pos)
  WHERE waitlist_pos IS NOT NULL;

CREATE TABLE waitlist_offers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  enrollment_id UUID NOT NULL REFERENCES enrollments(id),
  expires_at TIMESTAMPTZ NOT NULL,
  status offer_status NOT NULL DEFAULT 'pending'
);

CREATE TABLE court_bookings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(24) NOT NULL UNIQUE,
  court_id UUID NOT NULL REFERENCES courts(id),
  user_id UUID REFERENCES users(id),
  guest_name VARCHAR(120),
  guest_phone VARCHAR(16),
  start_at TIMESTAMPTZ NOT NULL,
  end_at TIMESTAMPTZ NOT NULL,
  status booking_status NOT NULL,
  channel VARCHAR(12) NOT NULL,
  price_vnd INT NOT NULL,
  discount_pct INT NOT NULL DEFAULT 0,
  vat_rate NUMERIC(5,2) NOT NULL DEFAULT 0,
  hold_until TIMESTAMPTZ,
  occupancy_id UUID UNIQUE REFERENCES occupancies(id),
  quota_hours NUMERIC(4,1) NOT NULL DEFAULT 0,
  CONSTRAINT booking_guest CHECK (user_id IS NOT NULL OR (guest_name IS NOT NULL AND guest_phone IS NOT NULL)),
  CONSTRAINT booking_occ_sync CHECK (
    (status IN ('hold','confirmed','in_use') AND occupancy_id IS NOT NULL)
    OR (status IN ('completed','cancelled','no_show') AND occupancy_id IS NULL)
  )
);
CREATE UNIQUE INDEX one_hold_per_user ON court_bookings (user_id)
  WHERE status = 'hold' AND user_id IS NOT NULL;
CREATE UNIQUE INDEX one_hold_per_guest_phone ON court_bookings (guest_phone)
  WHERE status = 'hold' AND guest_phone IS NOT NULL;

CREATE TABLE cashier_shifts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  receptionist_id UUID NOT NULL REFERENCES users(id),
  opened_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  closed_at TIMESTAMPTZ,
  cash_declared_vnd INT
);
CREATE UNIQUE INDEX one_open_shift ON cashier_shifts (receptionist_id)
  WHERE closed_at IS NULL;

CREATE TABLE payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(24) NOT NULL UNIQUE,
  user_id UUID REFERENCES users(id),
  shift_id UUID REFERENCES cashier_shifts(id),
  method pay_method NOT NULL,
  amount_vnd INT NOT NULL,
  vat_rate NUMERIC(5,2) NOT NULL,
  status pay_status NOT NULL DEFAULT 'posted',
  ref_type VARCHAR(24) NOT NULL,
  ref_id UUID NOT NULL,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT refund_amount_chk CHECK (
    (status IN ('refund_pending','refund_rejected') AND amount_vnd < 0)
    OR status = 'posted'
  )
);

CREATE VIEW v_subscription_debt AS
SELECT s.id AS subscription_id,
       mp.price_vnd - COALESCE(SUM(p.amount_vnd) FILTER (WHERE p.status = 'posted'), 0) AS debt_vnd
FROM subscriptions s
JOIN membership_plans mp ON mp.id = s.plan_id
LEFT JOIN payments p ON p.ref_type = 'subscription' AND p.ref_id = s.id
GROUP BY s.id, mp.price_vnd;

CREATE TABLE invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(24) NOT NULL UNIQUE,
  payment_id UUID NOT NULL UNIQUE REFERENCES payments(id),
  buyer_name VARCHAR(120) NOT NULL,
  buyer_tax_code VARCHAR(20),
  issued_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE invoice_lines (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID NOT NULL REFERENCES invoices(id),
  description TEXT NOT NULL,
  qty INT NOT NULL DEFAULT 1,
  unit_vnd INT NOT NULL,
  amount_vnd INT NOT NULL
);

CREATE TABLE equipment_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sku VARCHAR(24) NOT NULL UNIQUE,
  name VARCHAR(80) NOT NULL,
  sport sport_kind,
  stock INT NOT NULL DEFAULT 0,
  rent_vnd INT NOT NULL DEFAULT 0
);

CREATE TABLE equipment_loans (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  item_id UUID NOT NULL REFERENCES equipment_items(id),
  booking_id UUID REFERENCES court_bookings(id),
  phone VARCHAR(16) NOT NULL,
  qty INT NOT NULL,
  status loan_status NOT NULL DEFAULT 'out',
  due_at TIMESTAMPTZ NOT NULL,
  returned_at TIMESTAMPTZ
);

CREATE TABLE attendance (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  kind att_kind NOT NULL,
  user_id UUID NOT NULL REFERENCES users(id),
  session_id UUID REFERENCES sessions(id),
  result att_result,
  at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE training_plans (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  scope VARCHAR(16) NOT NULL,
  class_id UUID REFERENCES classes(id),
  user_id UUID REFERENCES users(id),
  source VARCHAR(8) NOT NULL DEFAULT 'coach',
  published BOOLEAN NOT NULL DEFAULT false,
  payload JSONB NOT NULL
);

CREATE TABLE tickets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  body TEXT NOT NULL,
  status VARCHAR(16) NOT NULL DEFAULT 'open',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE audit_logs (
  id BIGSERIAL PRIMARY KEY,
  at TIMESTAMPTZ NOT NULL DEFAULT now(),
  actor_id UUID,
  action VARCHAR(64) NOT NULL,
  entity VARCHAR(64) NOT NULL,
  entity_id UUID,
  before JSONB,
  after JSONB
);

CREATE TABLE feature_flags (
  key VARCHAR(16) PRIMARY KEY,
  enabled BOOLEAN NOT NULL DEFAULT false
);
INSERT INTO feature_flags(key, enabled) VALUES ('F4', false), ('F5', false), ('F6', false);

CREATE TABLE code_counters (
  kind VARCHAR(16) NOT NULL,
  yyyy INT NOT NULL,
  n INT NOT NULL,
  PRIMARY KEY (kind, yyyy)
);

CREATE TABLE idempotency_keys (
  key VARCHAR(80) PRIMARY KEY,
  user_id UUID,
  method VARCHAR(8) NOT NULL,
  path TEXT NOT NULL,
  request_hash TEXT NOT NULL,
  response_code INT NOT NULL,
  response_body JSONB NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL
);

CREATE TABLE sessions_auth (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id),
  token_hash TEXT NOT NULL UNIQUE,
  expires_at TIMESTAMPTZ NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE otp_challenges (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone VARCHAR(16) NOT NULL,
  purpose VARCHAR(16) NOT NULL,
  otp_hash TEXT NOT NULL,
  payload JSONB,
  expires_at TIMESTAMPTZ NOT NULL,
  attempts INT NOT NULL DEFAULT 0
);

CREATE TABLE outbox (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  channel VARCHAR(12) NOT NULL,
  template VARCHAR(32) NOT NULL,
  user_id UUID REFERENCES users(id),
  payload JSONB NOT NULL,
  dedupe_key VARCHAR(160) NOT NULL UNIQUE,
  sent_at TIMESTAMPTZ,
  attempts INT NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE VIEW inbox AS
  SELECT * FROM outbox WHERE sent_at IS NOT NULL AND channel = 'inapp';

-- ========== occupancy_* : API duy nhất chiếm/nhả sân ==========
CREATE OR REPLACE FUNCTION occupancy_attach(
  p_court UUID, p_start TIMESTAMPTZ, p_end TIMESTAMPTZ,
  p_kind occ_kind, p_ref UUID, p_reason TEXT DEFAULT NULL
) RETURNS UUID LANGUAGE plpgsql AS $$
DECLARE v_id UUID;
BEGIN
  INSERT INTO occupancies (court_id, start_at, end_at, kind, ref_id, reason)
  VALUES (p_court, p_start, p_end, p_kind, p_ref, p_reason)
  RETURNING id INTO v_id;
  RETURN v_id;
EXCEPTION WHEN exclusion_violation THEN
  RAISE EXCEPTION 'CONFLICT_SLOT' USING ERRCODE = '23P01';
END $$;

CREATE OR REPLACE FUNCTION occupancy_confirm_hold(p_id UUID) RETURNS VOID LANGUAGE plpgsql AS $$
BEGIN
  UPDATE occupancies SET kind = 'booking' WHERE id = p_id AND kind = 'hold';
  IF NOT FOUND THEN
    RAISE EXCEPTION 'HOLD_EXPIRED_OR_BAD_KIND';
  END IF;
END $$;

-- Job/hủy: NULL occupancy_id TRƯỚC, rồi DELETE occupancy (tránh FK chặn)
CREATE OR REPLACE FUNCTION occupancy_release_booking(p_booking UUID, p_terminal booking_status)
RETURNS VOID LANGUAGE plpgsql AS $$
DECLARE v_occ UUID;
BEGIN
  IF p_terminal NOT IN ('cancelled','no_show','completed') THEN
    RAISE EXCEPTION 'BAD_TERMINAL';
  END IF;
  SELECT occupancy_id INTO v_occ FROM court_bookings WHERE id = p_booking FOR UPDATE;
  UPDATE court_bookings SET occupancy_id = NULL, status = p_terminal WHERE id = p_booking;
  IF v_occ IS NOT NULL THEN
    DELETE FROM occupancies WHERE id = v_occ;
  END IF;
END $$;

CREATE OR REPLACE FUNCTION occupancy_release_session(p_session UUID, p_terminal session_status)
RETURNS VOID LANGUAGE plpgsql AS $$
DECLARE v_occ UUID;
BEGIN
  IF p_terminal NOT IN ('done','cancelled') THEN
    RAISE EXCEPTION 'BAD_TERMINAL';
  END IF;
  SELECT occupancy_id INTO v_occ FROM sessions WHERE id = p_session FOR UPDATE;
  UPDATE sessions SET occupancy_id = NULL, status = p_terminal WHERE id = p_session;
  IF v_occ IS NOT NULL THEN
    DELETE FROM occupancies WHERE id = v_occ;
  END IF;
  DELETE FROM coach_occupancies WHERE session_id = p_session;
END $$;

CREATE OR REPLACE FUNCTION occupancy_release_convert(p_group UUID) RETURNS VOID LANGUAGE plpgsql AS $$
BEGIN
  DELETE FROM occupancies WHERE convert_group_id = p_group;
END $$;

CREATE OR REPLACE FUNCTION occupancy_attach_convert(
  p_court UUID, p_start TIMESTAMPTZ, p_end TIMESTAMPTZ, p_ref UUID
) RETURNS UUID LANGUAGE plpgsql AS $$
DECLARE v_pair UUID; v_group UUID := gen_random_uuid(); v_id UUID;
BEGIN
  SELECT pair_court_id INTO v_pair FROM courts WHERE id = p_court;
  IF v_pair IS NULL THEN
    RAISE EXCEPTION 'COURT_NOT_CONVERTIBLE';
  END IF;
  INSERT INTO occupancies (court_id, start_at, end_at, kind, ref_id, convert_group_id)
  VALUES (p_court, p_start, p_end, 'convert', p_ref, v_group)
  RETURNING id INTO v_id;
  INSERT INTO occupancies (court_id, start_at, end_at, kind, ref_id, convert_group_id)
  VALUES (v_pair, p_start, p_end, 'convert', p_ref, v_group);
  RETURN v_id;
EXCEPTION WHEN exclusion_violation THEN
  DELETE FROM occupancies WHERE convert_group_id = v_group;
  RAISE EXCEPTION 'CONFLICT_SLOT' USING ERRCODE = '23P01';
END $$;

CREATE OR REPLACE FUNCTION booking_replace_hold(
  p_user UUID, p_phone VARCHAR, p_court UUID,
  p_start TIMESTAMPTZ, p_end TIMESTAMPTZ, p_booking UUID
) RETURNS UUID LANGUAGE plpgsql AS $$
DECLARE v_old UUID;
BEGIN
  SELECT id INTO v_old FROM court_bookings
  WHERE status = 'hold'
    AND (
      (p_user IS NOT NULL AND user_id = p_user)
      OR (p_phone IS NOT NULL AND guest_phone = p_phone)
    )
  FOR UPDATE;
  IF v_old IS NOT NULL THEN
    PERFORM occupancy_release_booking(v_old, 'cancelled');
  END IF;
  RETURN occupancy_attach(p_court, p_start, p_end, 'hold', p_booking, NULL);
END $$;

CREATE OR REPLACE FUNCTION normalize_phone(p TEXT) RETURNS VARCHAR
LANGUAGE sql IMMUTABLE AS $$
  SELECT CASE
    WHEN p ~ '^\+84' THEN p
    WHEN p ~ '^84' THEN '+' || p
    WHEN p ~ '^0' THEN '+84' || substr(p, 2)
    ELSE p
  END
$$;

CREATE OR REPLACE FUNCTION next_member_code() RETURNS VARCHAR
LANGUAGE plpgsql AS $$
DECLARE y INT := EXTRACT(YEAR FROM (now() AT TIME ZONE 'Asia/Ho_Chi_Minh'));
DECLARE n INT;
BEGIN
  INSERT INTO code_counters(kind, yyyy, n) VALUES ('member', y, 1)
  ON CONFLICT (kind, yyyy) DO UPDATE SET n = code_counters.n + 1
  RETURNING n INTO n;
  RETURN 'A3-' || y::text || '-' || lpad(n::text, 4, '0');
END $$;

CREATE OR REPLACE FUNCTION enrollment_confirm(p_class UUID, p_user UUID) RETURNS UUID
LANGUAGE plpgsql AS $$
DECLARE v_id UUID;
BEGIN
  PERFORM 1 FROM classes WHERE id = p_class FOR UPDATE;
  IF EXISTS (
    SELECT 1 FROM enrollments WHERE class_id = p_class AND user_id = p_user AND status = 'confirmed'
  ) THEN
    RAISE EXCEPTION 'ALREADY_ENROLLED';
  END IF;
  UPDATE classes SET enrolled_count = enrolled_count + 1
  WHERE id = p_class AND enrolled_count < capacity AND status = 'open';
  IF NOT FOUND THEN
    RAISE EXCEPTION 'CLASS_FULL';
  END IF;
  INSERT INTO enrollments (class_id, user_id, status)
  VALUES (p_class, p_user, 'confirmed')
  ON CONFLICT (class_id, user_id) DO NOTHING
  RETURNING id INTO v_id;
  IF v_id IS NULL THEN
    UPDATE classes SET enrolled_count = enrolled_count - 1 WHERE id = p_class;
    RAISE EXCEPTION 'ALREADY_ENROLLED';
  END IF;
  RETURN v_id;
END $$;

DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'arena3_app') THEN
    CREATE ROLE arena3_app LOGIN PASSWORD 'change_in_env';
  END IF;
END $$;
GRANT USAGE ON SCHEMA public TO arena3_app;
GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO arena3_app;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO arena3_app;
REVOKE INSERT, UPDATE, DELETE ON occupancies FROM PUBLIC;
REVOKE INSERT, UPDATE, DELETE ON occupancies FROM arena3_app;
GRANT SELECT ON occupancies TO arena3_app;
GRANT EXECUTE ON FUNCTION
  occupancy_attach(uuid, timestamptz, timestamptz, occ_kind, uuid, text),
  occupancy_confirm_hold(uuid),
  occupancy_release_booking(uuid, booking_status),
  occupancy_release_session(uuid, session_status),
  occupancy_release_convert(uuid),
  occupancy_attach_convert(uuid, timestamptz, timestamptz, uuid),
  booking_replace_hold(uuid, varchar, uuid, timestamptz, timestamptz, uuid),
  enrollment_confirm(uuid, uuid),
  next_member_code(),
  normalize_phone(text)
TO arena3_app;
```

# seed.sql
```sql
-- seed.sql — Arena3 SDD 0.3 — chạy sau DDL. Cấm bịa sân/giá/manager khác file này.
-- Manager login: +84900000001 / ChangeMe!a3 (cũng chấp nhận 0900000001 sau normalize_phone)

CREATE EXTENSION IF NOT EXISTS pgcrypto;

INSERT INTO users (id, full_name, name_normalized, phone, email, role, status, password_hash, pii_consent_at)
VALUES (
  '00000000-0000-0000-0000-000000000001',
  'Quản lý Arena3',
  'quan ly arena3',
  '+84900000001',
  'manager@arena3.local',
  'manager',
  'active',
  crypt('ChangeMe!a3', gen_salt('bf')),
  now()
);

INSERT INTO users (id, full_name, name_normalized, phone, role, status, password_hash, pii_consent_at)
VALUES (
  '00000000-0000-0000-0000-000000000002',
  'Lễ tân ca 1',
  'le tan ca 1',
  '+84900000002',
  'receptionist',
  'active',
  crypt('ChangeMe!a3', gen_salt('bf')),
  now()
);

INSERT INTO courts (id, court_code, sport, status, convertible, pair_court_id) VALUES
  ('10000000-0000-0000-0000-000000000001', 'CL-01', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000002', 'CL-02', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000003', 'CL-03', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000004', 'CL-04', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000005', 'CL-05', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000006', 'CL-06', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000007', 'CL-07', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000008', 'CL-08', 'badminton', 'ready', false, NULL),
  ('10000000-0000-0000-0000-000000000010', 'BR-01', 'basketball', 'ready', true, '10000000-0000-0000-0000-000000000011'),
  ('10000000-0000-0000-0000-000000000011', 'BC-01', 'volleyball', 'ready', true, '10000000-0000-0000-0000-000000000010');

-- Giá SRS: CL 80k/140k, BR 300k/500k, BC 250k/400k
INSERT INTO price_rules (sport, day_kind, start_local, end_local, price_vnd, is_peak) VALUES
  ('badminton', 'weekday', '06:00', '17:00', 80000, false),
  ('badminton', 'weekday', '17:00', '22:00', 140000, true),
  ('badminton', 'weekend', '06:00', '08:00', 80000, false),
  ('badminton', 'weekend', '08:00', '22:00', 140000, true),
  ('basketball', 'weekday', '06:00', '17:00', 300000, false),
  ('basketball', 'weekday', '17:00', '22:00', 500000, true),
  ('basketball', 'weekend', '06:00', '08:00', 300000, false),
  ('basketball', 'weekend', '08:00', '22:00', 500000, true),
  ('volleyball', 'weekday', '06:00', '17:00', 250000, false),
  ('volleyball', 'weekday', '17:00', '22:00', 400000, true),
  ('volleyball', 'weekend', '06:00', '08:00', 250000, false),
  ('volleyball', 'weekend', '08:00', '22:00', 400000, true);

-- holiday = copy weekend (lookup fallback holiday → weekend → weekday)
INSERT INTO price_rules (sport, day_kind, start_local, end_local, price_vnd, is_peak)
SELECT sport, 'holiday', start_local, end_local, price_vnd, is_peak
FROM price_rules WHERE day_kind = 'weekend';

INSERT INTO membership_plans (name, sport_scope, duration_days, session_quota, court_hours, court_discount_pct, price_vnd, is_on_sale) VALUES
  ('All-access 30 ngày', 'all', 30, NULL, 4, 15, 1500000, true),
  ('Cầu lông 30 ngày', 'badminton', 30, NULL, 2, 20, 800000, true),
  ('Cầu lông 10 buổi', 'badminton', NULL, 10, 0, 10, 1200000, true);

UPDATE feature_flags SET enabled = false WHERE key IN ('F4','F5','F6');
```

# openapi.yaml
```yaml
openapi: 3.0.3
info:
  title: Arena3 API slice 1
  version: 0.3.1
  description: >
    Sinh từ ENDPOINTS slice 1. Thiếu path trong file này = không implement.
    Auth Bearer only. Idempotency-Key bắt buộc trên POST bookings/confirm/payments/walk-in.
servers:
  - url: /v1
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  parameters:
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: true
      schema: { type: string, format: uuid }
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum: [UNAUTHENTICATED, FORBIDDEN, NOT_FOUND, CONFLICT_SLOT, HOLD_EXPIRED, CONFLICT_STATE, BR_VIOLATION, VALIDATION, RATE_LIMITED]
        message: { type: string }
        br: { type: string }
        requires_confirm: { type: boolean }
paths:
  /auth/register:
    post:
      summary: "Chỉ lưu otp_challenges (chưa INSERT users)"
      tags: [slice1]
      x-auth: public
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ full_name, phone, password, dob, guardian?, pii_consent }"
      responses:
        default:
          description: "202 { challenge_id }"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "422":
          description: VALIDATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /auth/otp/verify:
    post:
      summary: "OTP đúng mới INSERT users + phiên"
      tags: [slice1]
      x-auth: public
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ phone, otp }"
      responses:
        default:
          description: "200 { token, user }"
        "422":
          description: VALIDATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "429":
          description: RATE_LIMITED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /auth/login:
    post:
      summary: "Đăng nhập SĐT/email + MK"
      tags: [slice1]
      x-auth: public
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ login, password }"
      responses:
        default:
          description: "200 { token, user }"
        "401":
          description: UNAUTHENTICATED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "429":
          description: RATE_LIMITED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /auth/logout:
    post:
      summary: "Hủy phiên"
      tags: [slice1]
      x-auth: authenticated
      responses:
        default:
          description: "204"
  /auth/password/forgot:
    post:
      summary: "OTP quên MK (otp_challenges purpose=reset)"
      tags: [slice1]
      x-auth: public
      security: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ phone }"
      responses:
        default:
          description: "202 { challenge_id }"
        "429":
          description: RATE_LIMITED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /me:
    get:
      summary: "Hồ sơ + gói active"
      tags: [slice1]
      x-auth: authenticated
      responses:
        default:
          description: "200 { user, subscriptions }"
        "401":
          description: UNAUTHENTICATED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    patch:
      summary: "Sửa tên/health. Cấm phone/email (slice 2: POST /me/contact + OTP)"
      tags: [slice1]
      x-auth: authenticated
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ full_name, health_notes }"
      responses:
        default:
          description: "200"
        "422":
          description: VALIDATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /members:
    get:
      summary: "Tìm TV không dấu, từ 3 ký tự"
      tags: [slice1]
      x-auth: staff
      responses:
        default:
          description: "200 { items[] } P95<1s"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    post:
      summary: "Tạo TV tại quầy"
      tags: [slice1]
      x-auth: receptionist
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ full_name, phone, dob?, pii_consent }"
      responses:
        default:
          description: "201 { user }"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /members/{id}:
    get:
      summary: "Hồ sơ quầy: hạn, lịch hôm nay, nợ"
      tags: [slice1]
      x-auth: staff
      responses:
        default:
          description: "200"
        "404":
          description: NOT_FOUND
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /plans:
    get:
      summary: "Gói đang bán (TV) / tất cả (Manager)"
      tags: [slice1]
      x-auth: public
      security: []
      responses:
        default:
          description: "200 { items[] }"
    post:
      summary: "Tạo gói"
      tags: [slice1]
      x-auth: manager
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "MembershipPlan"
      responses:
        default:
          description: "201"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /plans/{id}:
    patch:
      summary: "Sửa gói — không hồi tố subscription cũ"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200"
        "404":
          description: NOT_FOUND
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /subscriptions:
    post:
      summary: "Pending cùng scope = cập nhật đơn cũ, không INSERT thứ hai"
      tags: [slice1]
      x-auth: member
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ plan_id }"
      responses:
        default:
          description: "201 { subscription, preview_end }"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /courts:
    get:
      summary: "Danh sách sân"
      tags: [slice1]
      x-auth: public
      security: []
      responses:
        default:
          description: "200"
  /occupancy:
    get:
      summary: "Heatmap ngày — mọi vai trò đã đăng nhập"
      tags: [slice1]
      x-auth: authenticated
      responses:
        default:
          description: "200 { date, slots: { court_id, start, kind, ref }[] }"
        "422":
          description: VALIDATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /bookings:
    post:
      summary: "Hold qua booking_replace_hold (hủy hold cũ cùng TX — TC-16)"
      tags: [slice1]
      x-auth: member
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ court_id, start_at, confirm_overlap? }"
      responses:
        default:
          description: "201 { booking, hold_until, price }"
        "409":
          description: CONFLICT_SLOT
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /bookings/{id}/confirm:
    post:
      summary: "Pay/quota (method=quota amount=0 posted, không cộng DT), hold→booking"
      tags: [slice1]
      x-auth: member
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ method, payment? }"
      responses:
        default:
          description: "200 { booking, payment }"
        "409":
          description: HOLD_EXPIRED
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /bookings/{id}/cancel:
    post:
      summary: "Hủy theo BR-33"
      tags: [slice1]
      x-auth: member
      responses:
        default:
          description: "200 { refund }"
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /bookings/{id}/check-in:
    post:
      summary: "Cửa sổ −15/+10"
      tags: [slice1]
      x-auth: receptionist
      responses:
        default:
          description: "200 in_use"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /walk-in:
    post:
      summary: "Nếu guest_phone = users.phone thì gắn user_id (một hold / SĐT)"
      tags: [slice1]
      x-auth: receptionist
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ court_id, start_at, guest_name, guest_phone, method }"
      responses:
        default:
          description: "201 { booking, payment }"
        "409":
          description: CONFLICT_SLOT
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /shifts/open:
    post:
      summary: "Mở ca quầy"
      tags: [slice1]
      x-auth: receptionist
      responses:
        default:
          description: "201 { shift }"
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /shifts/current:
    get:
      summary: "Ca đang open của lễ tân"
      tags: [slice1]
      x-auth: receptionist
      responses:
        default:
          description: "200 { shift }"
        "404":
          description: NOT_FOUND
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /shifts/{id}/close:
    post:
      summary: "Đóng ca + cash_declared"
      tags: [slice1]
      x-auth: receptionist
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ cash_declared_vnd }"
      responses:
        default:
          description: "200"
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /classes:
    get:
      summary: "Catalog lớp open — public, không PII HV"
      tags: [slice1]
      x-auth: public
      security: []
      responses:
        default:
          description: "200"
    post:
      summary: "Tạo lớp nháp"
      tags: [slice1]
      x-auth: manager
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "Class"
      responses:
        default:
          description: "201"
        "409":
          description: CONFLICT_SLOT
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /classes/{id}/publish:
    post:
      summary: "Generate session + occupancy + coach_occupancy"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200 { sessions }"
        "409":
          description: CONFLICT_SLOT
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /classes/{id}/enroll:
    post:
      summary: "enrollment_confirm. Staff gửi user_id; TV tự ghi danh"
      tags: [slice1]
      x-auth: staff_or_member
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ user_id? }"
      responses:
        default:
          description: "201 { enrollment }"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /enrollments/{id}:
    delete:
      summary: "Hủy ghi danh ≥ 4h"
      tags: [slice1]
      x-auth: member
      responses:
        default:
          description: "200"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /payments:
    post:
      summary: "Thu pending. LT bắt buộc shift_id ca open; Manager không cần ca"
      tags: [slice1]
      x-auth: desk
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ ref_type, ref_id, method, amount_vnd, shift_id? }"
      responses:
        default:
          description: "201 { payment, invoice }"
        "422":
          description: BR_VIOLATION
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /payments/{id}/refund:
    post:
      summary: "<1tr posted âm; ≥1tr refund_pending (không DELETE)"
      tags: [slice1]
      x-auth: receptionist
      requestBody:
        content:
          application/json:
            schema:
              type: object
              description: "{ amount_vnd, reason }"
      responses:
        default:
          description: "201 { payment }"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /payments/{id}/approve-refund:
    post:
      summary: "refund_pending → posted"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200 posted"
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /payments/{id}/reject-refund:
    post:
      summary: "refund_pending → refund_rejected; không vào DT; cấm DELETE"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200 refund_rejected"
        "409":
          description: CONFLICT_STATE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /invoices/{id}.pdf:
    get:
      summary: "PDF A5 / 80mm ?format="
      tags: [slice1]
      x-auth: staff
      responses:
        default:
          description: "200 application/pdf"
        "404":
          description: NOT_FOUND
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /reports/revenue:
    get:
      summary: "Doanh thu kỳ; tách quota"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200 { totals, by_source, by_sport }"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /reports/occupancy:
    get:
      summary: "Công suất sân"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /settings:
    get:
      summary: "Đọc center_settings"
      tags: [slice1]
      x-auth: staff
      responses:
        default:
          description: "200"
    patch:
      summary: "Sửa cấu hình — không hồi tố"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
  /price-rules:
    get:
      summary: "Bảng giá"
      tags: [slice1]
      x-auth: public
      security: []
      responses:
        default:
          description: "200"
    put:
      summary: "Thay bộ giá (giao dịch mới)"
      tags: [slice1]
      x-auth: manager
      responses:
        default:
          description: "200"
        "403":
          description: FORBIDDEN
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
```

*A3-SDD-001 v0.3.1 — kèm A3-SRS-001 v1.3.1*